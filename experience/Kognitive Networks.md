Architected a production-grade RAG search bot for complex internal documents, implementing semantic caching via the HNSW algorithm to optimize performance, significantly reduce latency, and cut inference costs by 25–30%.

Engineered an end-to-end QoE (Quality of Experience) forecasting system by transforming raw TCP/RTP streams into sliding-window aggregates. Developed an XGBoost classifier to predict churn risk based on jitter and throughput volatility, utilizing SHAP values to provide model interpretability for proactive network tuning.

Built a self-correcting AI Agent orchestration using n8n, OpenAI embeddings, and Pinecone. The pipeline automates support ticket triage by dynamically indexing resolved Jira tickets, resulting in a 65% reduction in MTTR and automating 40% of tier-1 queries, saving 20+ manual hours weekly.

Developed an AI-driven analytics engine on the Google ADK to optimize maritime network bandwidth. Implemented role-based behavioral profiling (Crew vs. Guest) to identify latency bottlenecks, enabling data-driven QoS policies that secured high-priority bandwidth for premium segments.